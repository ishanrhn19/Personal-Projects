print("Welcome to the tip calculator.")
initial_total= (float(input("What was the total bill? " )))
percentage_tip= (int(input("What percentage tip would you like to give? 10,12, or 15? " )))
number_of_people=(int(input("How many people to split the bill? " )))
bill_with_tip= percentage_tip/100 * initial_total + initial_total
result= bill_with_tip/number_of_people
final_result= (round(result,3))
print(f"Each person should pay ${final_result}")
